---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const highlights = [
  { icon: "🔍", text: "3 Job Boards" },
  { icon: "🤖", text: "AI-Scored Matches" },
  { icon: "📄", text: "Tailored Resumes" },
  { icon: "🛡️", text: "Human-in-the-Loop" },
];
---

<section
  id="stats"
  class:list={[
    "py-10 bg-gradient-to-r from-primary-800 via-primary-700 to-primary-800 text-white relative overflow-hidden",
    className,
  ]}
  data-animate
>
  <!-- Shine sweep overlay -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div
      class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent"
      style="animation: shine-sweep 1.5s ease-out 0.5s 1 both;"
    >
    </div>
  </div>

  <div class="max-w-5xl mx-auto px-4 relative z-10">
    <div class="flex flex-wrap justify-center gap-x-12 gap-y-4 text-center">
      {highlights.map((item) => (
        <div class="flex items-center gap-2.5">
          <span class="text-xl">{item.icon}</span>
          <span class="font-display font-semibold text-sm md:text-base tracking-wide uppercase text-white/90">
            {item.text}
          </span>
        </div>
      ))}
    </div>
  </div>
</section>
