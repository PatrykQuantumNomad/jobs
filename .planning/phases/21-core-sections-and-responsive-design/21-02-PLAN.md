---
phase: 21-core-sections-and-responsive-design
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - site/src/components/sections/Features.astro
  - site/src/components/sections/TechStack.astro
  - site/src/components/sections/Footer.astro
  - site/src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Feature grid displays 8 cards with icons and descriptions organized by 4 categories"
    - "Tech stack section shows 6 technology badges with name and one-word role labels"
    - "Footer contains GitHub repo link and personal site link (patrykgolabek.dev)"
    - "Page is usable at 375px, 768px, and 1440px without horizontal scroll or overlapping elements"
  artifacts:
    - path: "site/src/components/sections/Features.astro"
      provides: "Feature grid with 8 cards in responsive grid layout"
      contains: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - path: "site/src/components/sections/TechStack.astro"
      provides: "Tech stack badges with role labels"
      contains: "Built With"
    - path: "site/src/components/sections/Footer.astro"
      provides: "Footer with GitHub and personal site links"
      contains: "patrykgolabek.dev"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Features.astro"
      via: "Astro component import"
      pattern: "import Features"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/TechStack.astro"
      via: "Astro component import"
      pattern: "import TechStack"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Footer.astro"
      via: "Astro component import"
      pattern: "import Footer"
    - from: "site/src/components/sections/Footer.astro"
      to: "https://github.com/patrykgolabek/jobs"
      via: "anchor href"
      pattern: "github.com/patrykgolabek/jobs"
    - from: "site/src/components/sections/Footer.astro"
      to: "https://patrykgolabek.dev"
      via: "anchor href"
      pattern: "patrykgolabek.dev"
---

<objective>
Create the below-the-fold content: Features grid with 8 categorized cards, Tech Stack badges, and Footer with external links. Complete the full page composition and verify responsive layout at all breakpoints.

Purpose: Deliver the remaining content sections that communicate project depth (features, technology choices) and provide navigation/contact (footer), completing the full single-page layout.
Output: Features.astro, TechStack.astro, Footer.astro, final index.astro composing all 5 sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-core-sections-and-responsive-design/21-01-SUMMARY.md
@.planning/phases/21-core-sections-and-responsive-design/21-RESEARCH.md
@site/src/styles/global.css
@site/src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Features grid and TechStack badges sections</name>
  <files>
    site/src/components/sections/Features.astro
    site/src/components/sections/TechStack.astro
  </files>
  <action>
    Create `site/src/components/sections/Features.astro` (CONT-03):
    - Import 8 SVG icons from `../../icons/` using Astro native SVG import: SearchIcon (search.svg), FilterIcon (filter.svg), BrainIcon (brain.svg), FileTextIcon (file-text.svg), LayoutIcon (layout.svg), BarChartIcon (bar-chart.svg), ZapIcon (zap.svg), ShieldIcon (shield.svg)
    - Define features data array with 8 items, 2 per category:
      - Discovery: "Multi-Platform Scraping" (search icon) -- "Scrapes Indeed, Dice, and RemoteOK with Playwright stealth automation. Handles login, pagination, and deduplication." AND "Smart Filtering" (filter icon) -- "Configurable search queries with location, salary, and keyword filters. YAML-driven configuration, no code changes needed."
      - Intelligence: "AI-Powered Scoring" (brain icon) -- "Claude CLI scores each job 1-5 against your candidate profile with explainable breakdowns and weighted criteria." AND "Resume Tailoring" (file-text icon) -- "Generates role-specific resumes via Claude with anti-fabrication validation. Never invents experience or qualifications."
      - Dashboard: "Web Dashboard" (layout icon) -- "FastAPI + htmx real-time dashboard with Kanban board, search management, and one-click apply controls." AND "Analytics & Insights" (bar-chart icon) -- "Score distribution, platform breakdown, salary ranges, and application funnel metrics at a glance."
      - Automation: "One-Click Apply" (zap icon) -- "Background apply engine with SSE progress streaming and human-in-the-loop confirmation gates." AND "Safety First" (shield icon) -- "Never auto-submits without approval. CAPTCHA detection, credential gating, and screenshot debugging built in."
    - Map each feature to its icon component. Use a lookup object or direct mapping in the array (e.g., store the icon component reference in the data array).
    - Section: `id="features"`, `py-20 bg-white`
    - Container: `max-w-6xl mx-auto px-4`
    - Section heading: `font-display text-3xl font-bold text-surface-900 text-center` with text "What It Does"
    - Subheading: `mt-3 text-surface-600 text-center max-w-2xl mx-auto` with text "A complete pipeline from job discovery to application submission"
    - Grid: `mt-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` (use CSS Grid not Flexbox for equal-height rows)
    - Each card: `p-6 rounded-xl bg-surface-50 border border-surface-200 hover:border-primary-300 hover:shadow-sm transition-all`
    - Card icon container: `w-10 h-10 rounded-lg bg-primary-100 flex items-center justify-center text-primary-600` with the icon component rendered at width=20, height=20
    - Card title: `mt-4 font-display text-lg font-semibold text-surface-900`
    - Card category tag: `mt-1 text-xs text-primary-500 uppercase tracking-wider font-semibold` showing the category name
    - Card description: `mt-2 text-sm text-surface-600 leading-relaxed`

    Create `site/src/components/sections/TechStack.astro` (CONT-04):
    - Import icons: BrainIcon (brain.svg for Claude CLI), DatabaseIcon (database.svg), CodeIcon (code.svg), TerminalIcon (terminal.svg), LayoutIcon (layout.svg), ZapIcon (zap.svg)
    - Define technologies array with 6 items: `[{ name: "Python 3.14", role: "Runtime", icon: TerminalIcon }, { name: "Playwright", role: "Automation", icon: ZapIcon }, { name: "FastAPI", role: "Backend", icon: CodeIcon }, { name: "SQLite", role: "Database", icon: DatabaseIcon }, { name: "htmx", role: "Frontend", icon: LayoutIcon }, { name: "Claude CLI", role: "AI Engine", icon: BrainIcon }]`
    - Section: `id="tech-stack"`, `py-20 bg-surface-50`
    - Container: `max-w-6xl mx-auto px-4 text-center`
    - Section heading: `font-display text-3xl font-bold text-surface-900` with text "Built With"
    - Subheading: `mt-3 text-surface-600` with text "The tools that power the pipeline"
    - Badge container: `mt-12 flex flex-wrap justify-center gap-6`
    - Each badge: `flex flex-col items-center gap-2 px-6 py-4 rounded-xl bg-white border border-surface-200 shadow-sm hover:shadow-md hover:border-primary-200 transition-all`
    - Badge icon: `w-12 h-12 rounded-lg bg-primary-50 flex items-center justify-center text-primary-600` with icon at width=24, height=24
    - Badge name: `font-display font-semibold text-surface-900`
    - Badge role: `text-xs text-surface-500 uppercase tracking-wider`
    - No `<style>` blocks -- utility classes only
  </action>
  <verify>
    Both files exist and contain proper Astro frontmatter with icon imports.
    Run `cd /Users/patrykattc/work/jobs/site && npx astro check` -- no type errors.
    Features.astro contains all 8 feature titles. TechStack.astro contains all 6 technology names.
  </verify>
  <done>
    Features section renders 8 cards in a responsive grid (1-col mobile, 2-col tablet, 3-col desktop) with icons, category tags, titles, and descriptions.
    TechStack section renders 6 technology badges with icons, names, and one-word role labels in a centered flex-wrap layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Footer, complete page composition, and verify responsive build</name>
  <files>
    site/src/components/sections/Footer.astro
    site/src/pages/index.astro
  </files>
  <action>
    Create `site/src/components/sections/Footer.astro` (CONT-09):
    - Import GithubIcon from `../../icons/github.svg`
    - Define constants: `github = "https://github.com/patrykgolabek/jobs"`, `personalSite = "https://patrykgolabek.dev"`, `currentYear = new Date().getFullYear()`
    - Footer element (NOT section): `<footer id="footer" class="py-12 bg-surface-900 text-surface-300">`
    - Container: `max-w-6xl mx-auto px-4`
    - Top row: `flex flex-col md:flex-row md:items-center md:justify-between gap-6`
    - Left side: "JobFlow" brand name in `font-display text-lg font-semibold text-white`, tagline "From discovery to application in one tool." in `mt-1 text-sm text-surface-400`
    - Right side: `flex items-center gap-6`
      - GitHub link: `<a>` to GitHub URL with `target="_blank" rel="noopener noreferrer"` and `class="text-surface-400 hover:text-white transition-colors"`. Contains GithubIcon (width=20, height=20, fill="currentColor") and `<span class="sr-only">GitHub</span>` for accessibility.
      - Personal site link: `<a>` to personalSite with `target="_blank" rel="noopener noreferrer"` and `class="text-sm text-surface-400 hover:text-white transition-colors"`. Text: "patrykgolabek.dev".
    - Bottom divider: `mt-8 pt-8 border-t border-surface-700 text-center text-sm text-surface-500` with copyright text including dynamic year

    Update `site/src/pages/index.astro` to compose ALL 5 sections:
    - Import all section components: Hero, Stats, Features, TechStack, Footer
    - Keep BaseLayout with existing title/description props
    - Render in order: `<Hero />`, `<Stats />`, `<Features />`, `<TechStack />`, `<Footer />`
    - No wrapping `<main>` needed -- each section handles its own semantic structure. However, wrap Hero through TechStack in `<main>` for semantic HTML, and Footer stays outside main.
    - Structure:
      ```
      <BaseLayout>
        <main>
          <Hero />
          <Stats />
          <Features />
          <TechStack />
        </main>
        <Footer />
      </BaseLayout>
      ```

    After composing, verify responsive behavior (DSGN-02):
    - Run `npm run build` to confirm full build succeeds
    - Inspect the built HTML to confirm all sections are present: hero headline, stats numbers, feature card titles, tech badge names, footer links
    - Confirm no hardcoded widths that would cause horizontal scroll -- all containers use `max-w-6xl mx-auto px-4` and responsive grid classes
  </action>
  <verify>
    Run `cd /Users/patrykattc/work/jobs/site && npm run build` -- build completes with zero errors.
    Run `cd /Users/patrykattc/work/jobs/site && npx astro check` -- no type errors.
    Verify built HTML at `site/dist/jobs/index.html` contains: "What It Does" (features heading), "Built With" (tech heading), "patrykgolabek.dev" (footer), "github.com/patrykgolabek/jobs" (footer link).
    All external links (`<a>` with `target="_blank"`) also have `rel="noopener noreferrer"`.
    No `<style>` blocks in any section component.
  </verify>
  <done>
    Footer renders with GitHub icon link, personal site link, brand name, tagline, and copyright.
    index.astro composes all 5 sections (Hero, Stats, Features, TechStack, Footer) in proper order inside BaseLayout.
    Full site builds without errors.
    All external links have `target="_blank" rel="noopener noreferrer"`.
    Responsive layout uses mobile-first Tailwind classes: single column at 375px, 2-col grids at 768px (md:), 3-col grids at 1440px (lg:+).
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/patrykattc/work/jobs/site && npm run build` completes with zero errors
2. `npx astro check` reports no type errors
3. Built HTML at `site/dist/jobs/index.html` contains all 5 sections with expected content
4. All 8 feature card titles present in built HTML
5. All 6 tech stack badge names present in built HTML
6. Footer contains links to `github.com/patrykgolabek/jobs` and `patrykgolabek.dev`
7. All `target="_blank"` links have `rel="noopener noreferrer"`
8. No `<style>` blocks in any component (utility classes only per Tailwind v4 recommendation)
9. Responsive classes present: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (features), `grid-cols-2 md:grid-cols-4` (stats), `flex-col md:flex-row` (footer)
</verification>

<success_criteria>
- Features section shows 8 cards in responsive grid with icons, categories, titles, descriptions
- TechStack section shows 6 badges with icons, names, and role labels
- Footer shows GitHub link, personal site link, brand, and copyright
- Complete page renders all 5 sections in order: Hero, Stats, Features, TechStack, Footer
- Site builds without errors
- Layout works at 375px (1-col stacking), 768px (2-col grids), 1440px (3-col grids) without horizontal scroll
</success_criteria>

<output>
After completion, create `.planning/phases/21-core-sections-and-responsive-design/21-02-SUMMARY.md`
</output>
