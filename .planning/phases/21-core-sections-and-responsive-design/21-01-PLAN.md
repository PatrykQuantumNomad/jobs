---
phase: 21-core-sections-and-responsive-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/ui/ScreenshotFrame.astro
  - site/src/components/sections/Hero.astro
  - site/src/components/sections/Stats.astro
  - site/src/icons/search.svg
  - site/src/icons/filter.svg
  - site/src/icons/brain.svg
  - site/src/icons/file-text.svg
  - site/src/icons/layout.svg
  - site/src/icons/bar-chart.svg
  - site/src/icons/zap.svg
  - site/src/icons/shield.svg
  - site/src/icons/github.svg
  - site/src/icons/database.svg
  - site/src/icons/code.svg
  - site/src/icons/terminal.svg
  - site/src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Visitor sees headline, subheadline, and View on GitHub button above the fold"
    - "Dashboard screenshot area renders inside a browser mockup frame with traffic light dots"
    - "Stats bar displays 4 metrics (18K+ LOC, 581 Tests, 80%+ Coverage, 3 Platforms) in a distinct row"
  artifacts:
    - path: "site/src/components/ui/ScreenshotFrame.astro"
      provides: "Browser mockup frame with title bar dots and slotted content area"
      contains: "aspect-video"
    - path: "site/src/components/sections/Hero.astro"
      provides: "Hero section with headline, subheadline, CTA button, ScreenshotFrame"
      contains: "View on GitHub"
    - path: "site/src/components/sections/Stats.astro"
      provides: "Stats bar with 4 metrics in responsive grid"
      contains: "grid-cols-2"
    - path: "site/src/icons/github.svg"
      provides: "GitHub icon SVG with currentColor fill"
      contains: "currentColor"
  key_links:
    - from: "site/src/components/sections/Hero.astro"
      to: "site/src/components/ui/ScreenshotFrame.astro"
      via: "Astro component import"
      pattern: "import ScreenshotFrame"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Hero.astro"
      via: "Astro component import"
      pattern: "import Hero"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Stats.astro"
      via: "Astro component import"
      pattern: "import Stats"
---

<objective>
Create the above-the-fold content: ScreenshotFrame browser mockup component, SVG icon library, Hero section with CTA, and Stats bar with project metrics.

Purpose: Establish the first impression -- headline, dashboard mockup, and credibility metrics that communicate project quality within seconds.
Output: ScreenshotFrame.astro, Hero.astro, Stats.astro, 12 SVG icons, updated index.astro composing the first two sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-and-configuration/20-01-SUMMARY.md
@.planning/phases/21-core-sections-and-responsive-design/21-RESEARCH.md
@site/src/layouts/BaseLayout.astro
@site/src/styles/global.css
@site/src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScreenshotFrame component and SVG icon library</name>
  <files>
    site/src/components/ui/ScreenshotFrame.astro
    site/src/icons/search.svg
    site/src/icons/filter.svg
    site/src/icons/brain.svg
    site/src/icons/file-text.svg
    site/src/icons/layout.svg
    site/src/icons/bar-chart.svg
    site/src/icons/zap.svg
    site/src/icons/shield.svg
    site/src/icons/github.svg
    site/src/icons/database.svg
    site/src/icons/code.svg
    site/src/icons/terminal.svg
  </files>
  <action>
    Create `site/src/components/ui/ScreenshotFrame.astro`:
    - Props interface: `url?: string` (default "localhost:8000/dashboard"), `class?: string`
    - Outer div: `rounded-lg overflow-hidden shadow-2xl border border-surface-200` with `class:list` merging
    - Title bar: `flex items-center gap-2 px-4 py-3 bg-surface-100 border-b border-surface-200`
    - Traffic light dots: three spans with `w-3 h-3 rounded-full` and `bg-red-400`, `bg-yellow-400`, `bg-green-400` (Tailwind v4 defaults, NOT overridden by @theme)
    - URL text: `text-xs text-surface-400 font-mono` centered in title bar
    - Content area: `aspect-video bg-gradient-to-br from-primary-600/10 via-primary-400/20 to-primary-700/10` with a `<slot />` for future screenshot content
    - No `<style>` blocks -- utility classes only

    Create 12 SVG icon files in `site/src/icons/`. Each SVG must:
    - Be a minimal, single-path or few-path icon at 24x24 viewBox
    - Use `fill="none"` on the root SVG element
    - Use `stroke="currentColor"` with `stroke-width="2"` `stroke-linecap="round"` `stroke-linejoin="round"` (Lucide/Feather style)
    - No hardcoded colors -- `currentColor` only so Tailwind text color classes work
    - Keep SVGs simple and clean (no metadata, no editor cruft)

    Icon designs (Feather/Lucide style, stroke-based):
    - `search.svg`: Magnifying glass (circle + diagonal line)
    - `filter.svg`: Funnel shape (three horizontal lines decreasing in width)
    - `brain.svg`: Simplified brain outline (curved paths suggesting two hemispheres)
    - `file-text.svg`: Document with horizontal lines (rectangle with corner fold + lines)
    - `layout.svg`: Dashboard layout grid (rectangle divided into sections)
    - `bar-chart.svg`: Three vertical bars of increasing height
    - `zap.svg`: Lightning bolt
    - `shield.svg`: Shield outline
    - `github.svg`: GitHub octocat logo mark (use the standard GitHub mark path, `fill="currentColor"` instead of stroke since it's a filled logo)
    - `database.svg`: Cylinder/database icon (ellipse top + sides + bottom)
    - `code.svg`: Angle brackets `< />` (two chevrons)
    - `terminal.svg`: Terminal prompt (rectangle with `>_` inside)
  </action>
  <verify>
    All 12 SVG files exist in `site/src/icons/` and each contains `currentColor`.
    ScreenshotFrame.astro exists at `site/src/components/ui/ScreenshotFrame.astro` and contains `aspect-video` and `slot`.
    Run `cd /Users/patrykattc/work/jobs/site && npx astro check` to verify no type errors.
  </verify>
  <done>
    ScreenshotFrame component renders a browser mockup with traffic light dots, URL bar, and gradient placeholder content area.
    12 SVG icons are available for import as Astro components using native SVG import (Astro 5.7+).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Hero and Stats sections, compose in index.astro</name>
  <files>
    site/src/components/sections/Hero.astro
    site/src/components/sections/Stats.astro
    site/src/pages/index.astro
  </files>
  <action>
    Create `site/src/components/sections/Hero.astro` (CONT-01 + DSGN-04):
    - Import ScreenshotFrame from `../ui/ScreenshotFrame.astro`
    - Import GithubIcon from `../../icons/github.svg`
    - Section with `id="hero"`, padding `py-16 md:py-24 lg:py-32`
    - Container: `max-w-6xl mx-auto px-4 md:px-8`
    - Responsive layout: `flex flex-col lg:flex-row lg:items-center lg:gap-12`
    - Left column (`lg:w-1/2`):
      - Headline: `font-display text-4xl md:text-5xl lg:text-6xl font-bold text-surface-900 leading-tight` with text "Job Search, Automated"
      - Subheadline: `mt-4 text-lg md:text-xl text-surface-600 leading-relaxed` with text "Self-hosted pipeline that scrapes job boards, scores matches against your profile, and automates applications with human-in-the-loop safety."
      - CTA container: `mt-8 flex flex-wrap gap-4`
      - Primary CTA: `<a>` styled as button linking to `https://github.com/patrykgolabek/jobs` with `target="_blank" rel="noopener noreferrer"`. Style: `inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-primary-600 text-white font-semibold hover:bg-primary-700 transition-colors`. Text: "View on GitHub" with GithubIcon (width=20, height=20, fill="currentColor").
      - Secondary CTA: `<a href="#features">` styled: `inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-surface-300 text-surface-700 font-semibold hover:border-surface-400 hover:text-surface-900 transition-colors`. Text: "See Features".
    - Right column (`mt-10 lg:mt-0 lg:w-1/2`):
      - `<ScreenshotFrame />` with default props (no url override needed)

    Create `site/src/components/sections/Stats.astro` (CONT-02):
    - Props interface: `class?: string`
    - Data array: `[{ value: "18K+", label: "Lines of Code" }, { value: "581", label: "Tests" }, { value: "80%+", label: "Coverage" }, { value: "3", label: "Platforms" }]`
    - Section with `id="stats"`, classes: `py-12 bg-primary-900 text-white` merged with className via `class:list`
    - Container: `max-w-6xl mx-auto px-4`
    - Grid: `grid grid-cols-2 md:grid-cols-4 gap-8 text-center` (2x2 on mobile to avoid text overflow at 375px, 4-col row on tablet+)
    - Each stat: value in `font-display text-4xl font-bold`, label in `mt-1 text-sm text-primary-200 uppercase tracking-wider`

    Update `site/src/pages/index.astro`:
    - Import Hero and Stats sections
    - Replace the current `<main>` placeholder content with `<Hero />` followed by `<Stats />`
    - Keep BaseLayout wrapper with same title and description props
    - Structure: `<BaseLayout><Hero /><Stats /></BaseLayout>` (more sections added in Plan 02)
  </action>
  <verify>
    Run `cd /Users/patrykattc/work/jobs/site && npm run build` -- build completes with zero errors.
    Run `cd /Users/patrykattc/work/jobs/site && npx astro check` -- no type errors.
    Verify the built HTML contains: "View on GitHub", "Job Search, Automated", "18K+", "581", "80%+", "3 Platforms" text.
  </verify>
  <done>
    Hero section displays headline ("Job Search, Automated"), subheadline, GitHub CTA button with icon, "See Features" secondary link, and ScreenshotFrame browser mockup.
    Stats bar shows 4 metrics in a 2-col mobile / 4-col tablet+ grid with dark primary background.
    index.astro composes Hero and Stats sections inside BaseLayout.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/patrykattc/work/jobs/site && npm run build` completes with zero errors
2. `npx astro check` reports no type errors
3. Built HTML at `site/dist/jobs/index.html` contains all expected text content
4. ScreenshotFrame renders with traffic light dots (red, yellow, green) and gradient content area
5. GitHub CTA links to `https://github.com/patrykgolabek/jobs` with `target="_blank" rel="noopener noreferrer"`
</verification>

<success_criteria>
- Hero section renders with headline, subheadline, two CTA buttons, and browser mockup frame
- Stats bar renders with 4 metrics in responsive grid
- 12 SVG icons are importable as Astro components
- Site builds without errors
- All external links have proper security attributes
</success_criteria>

<output>
After completion, create `.planning/phases/21-core-sections-and-responsive-design/21-01-SUMMARY.md`
</output>
