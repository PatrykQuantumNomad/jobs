---
phase: 22-engineering-depth-sections
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - site/src/components/sections/Timeline.astro
  - site/src/components/sections/QuickStart.astro
  - site/src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "A milestone timeline displays v1.0, v1.1, and v1.2 with dates, stats, and feature highlights telling the build story"
    - "A quick start section presents 5 numbered steps with syntax-highlighted bash commands that a developer could follow"
    - "The full page composes all 9 sections (Hero, Stats, Features, TechStack, Architecture, CodeSnippets, Timeline, QuickStart, Footer) in order without errors"
    - "Adjacent sections have alternating or distinct background colors with no visual merging"
  artifacts:
    - path: "site/src/components/sections/Timeline.astro"
      provides: "Milestone timeline with v1.0, v1.1, v1.2 data"
      contains: "id=\"timeline\""
    - path: "site/src/components/sections/QuickStart.astro"
      provides: "5-step setup guide with bash code blocks"
      contains: "id=\"quickstart\""
    - path: "site/src/pages/index.astro"
      provides: "Full page composition with all 9 sections"
      contains: "Architecture"
  key_links:
    - from: "site/src/components/sections/QuickStart.astro"
      to: "astro:components"
      via: "Astro built-in Code component"
      pattern: "import.*Code.*from.*astro:components"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Architecture.astro"
      via: "Astro component import"
      pattern: "import Architecture"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/CodeSnippets.astro"
      via: "Astro component import"
      pattern: "import CodeSnippets"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/Timeline.astro"
      via: "Astro component import"
      pattern: "import Timeline"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/sections/QuickStart.astro"
      via: "Astro component import"
      pattern: "import QuickStart"
---

<objective>
Create the Timeline section (CONT-07) and Quick Start section (CONT-08), then compose all 4 new sections into the page alongside existing Phase 21 sections -- completing all Engineering Depth content.

Purpose: Timeline tells the build story (velocity, iteration, growth), Quick Start gives an actionable entry point for interested developers, and page composition delivers the complete reading experience.
Output: Two new section components + updated index.astro with all 9 sections rendering in correct order.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-engineering-depth-sections/22-RESEARCH.md
@.planning/phases/22-engineering-depth-sections/22-01-SUMMARY.md

Existing page to modify:
@site/src/pages/index.astro

Existing section patterns:
@site/src/components/sections/Features.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Timeline and QuickStart sections</name>
  <files>
    site/src/components/sections/Timeline.astro
    site/src/components/sections/QuickStart.astro
  </files>
  <action>
    **Timeline.astro (CONT-07):**

    1. Create `site/src/components/sections/Timeline.astro` with `id="timeline"` and `bg-surface-50` background
    2. Heading: "Built in Public" with subtitle "From MVP to production in one week"
    3. Define `milestones` data array with verified data from PROJECT.md:
       - v1.0 MVP (Feb 8, 2026): "6,705 LOC | 8 phases | 24 plans" -- Full pipeline: scrape, score, dashboard, AI resume, apply. Features: Multi-platform scraping, Scoring engine, Web dashboard, AI resume tailoring, One-click apply
       - v1.1 Test Suite (Feb 8, 2026): "5,639 test LOC | 428 tests | 80%+ coverage" -- Comprehensive test infrastructure with CI pipeline. Features: Unit tests, Integration tests, E2E browser tests, GitHub Actions CI
       - v1.2 Claude CLI (Feb 11, 2026): "581 tests | 15 requirements | 7 plans" -- Replaced SDK with Claude CLI subprocess for all AI features. Features: Claude CLI wrapper, AI scoring, SSE streaming, Anti-fabrication validation
    4. Layout: Left-aligned vertical timeline
       - A vertical line on the left using `border-l-2 border-primary-200` on a container div
       - Each milestone: a row with a version badge/dot on the line (absolute positioned or using negative margin-left) + content card to the right
       - Version badge: `bg-primary-600 text-white rounded-full px-3 py-1 text-sm font-display font-bold` (shows "v1.0", "v1.1", "v1.2")
       - Content: title + date + stats line + description + feature tags/pills
       - Stats in `text-sm text-surface-500 font-mono`
       - Feature tags as small pills: `bg-primary-50 text-primary-700 rounded-full px-2 py-0.5 text-xs` with `flex-wrap gap-2`
       - Spacing: `space-y-12` between milestones
    5. Container: `max-w-3xl mx-auto px-4 py-20` (narrower than 6xl since timeline is text-heavy)
    6. Mobile: Same vertical layout works at all sizes (no breakpoint changes needed for vertical timeline)

    **QuickStart.astro (CONT-08):**

    1. Create `site/src/components/sections/QuickStart.astro` with `id="quickstart"` and `bg-white` background
    2. Import `{ Code } from 'astro:components'`; define `const THEME = "github-dark-default"`
    3. Heading: "Quick Start" with subtitle "Up and running in five steps"
    4. Define `steps` data array with 5 entries:
       - Step 1: "Clone the repository" -- `git clone https://github.com/patrykgolabek/jobs.git && cd jobs` (lang: bash)
       - Step 2: "Install dependencies" -- `pip install -r requirements.txt && playwright install chromium` (lang: bash)
       - Step 3: "Configure your profile" -- `cp config.example.yaml config.yaml\n# Edit config.yaml with your search queries, target titles, and tech keywords` (lang: bash)
       - Step 4: "Run the pipeline" -- `python orchestrator.py --platforms indeed remoteok` (lang: bash)
       - Step 5: "Open the dashboard" -- `python -m webapp.app\n# Visit http://localhost:8000` (lang: bash)
    5. Each step renders as:
       - Number circle: `w-8 h-8 rounded-full bg-primary-600 text-white flex items-center justify-center font-display font-bold text-sm shrink-0`
       - Title: `font-display font-semibold text-surface-900`
       - Code block: `ml-11 rounded-lg overflow-hidden` wrapper around `<Code code={step.code} lang={step.lang} theme={THEME} />`
       - Stack steps with `space-y-8`
    6. Container: `max-w-3xl mx-auto px-4 py-20` (narrower, matches timeline width)

    **Background color plan for the full page (verify no adjacent sections share the same background):**
    - Hero: bg-surface-50 (body default)
    - Stats: bg-primary-900 (dark accent)
    - Features: bg-white
    - TechStack: bg-surface-50
    - Architecture: bg-white
    - CodeSnippets: bg-surface-900 (dark)
    - Timeline: bg-surface-50
    - QuickStart: bg-white
    - Footer: bg-surface-900 (dark)

    Note: TechStack(bg-surface-50) -> Architecture(bg-white) -> CodeSnippets(bg-surface-900) -> Timeline(bg-surface-50) -> QuickStart(bg-white) -> Footer(bg-surface-900) -- all transitions are between distinct backgrounds.
  </action>
  <verify>
    Run `cd /Users/patrykattc/work/jobs/site && npm run build 2>&1` -- should complete with zero errors. Grep built HTML for "Built in Public" and all 3 milestone versions ("v1.0", "v1.1", "v1.2"). Grep for "Quick Start" and all 5 step titles. Verify `<Code />` component integration by checking for `shiki` class in output.
  </verify>
  <done>Timeline section renders 3 milestones with version badges, dates, stats, and feature pills in a vertical timeline layout. QuickStart section renders 5 numbered steps with syntax-highlighted bash commands. Both build without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Compose all sections into index.astro and verify full build</name>
  <files>site/src/pages/index.astro</files>
  <action>
    Update `site/src/pages/index.astro` to import and render all 4 new Phase 22 sections:

    1. Add imports for the 4 new section components (after existing TechStack import, before Footer import):
       ```
       import Architecture from "../components/sections/Architecture.astro";
       import CodeSnippets from "../components/sections/CodeSnippets.astro";
       import Timeline from "../components/sections/Timeline.astro";
       import QuickStart from "../components/sections/QuickStart.astro";
       ```

    2. Add the 4 new sections inside `<main>` after `<TechStack />` and before the closing `</main>`:
       ```
       <Architecture />
       <CodeSnippets />
       <Timeline />
       <QuickStart />
       ```

    3. Final section order inside `<main>`:
       Hero -> Stats -> Features -> TechStack -> Architecture -> CodeSnippets -> Timeline -> QuickStart

    4. Footer remains outside `<main>` (semantic HTML landmark pattern from Phase 21).

    5. Do NOT change the `<BaseLayout>` title or description props.
  </action>
  <verify>
    Run `cd /Users/patrykattc/work/jobs/site && npm run build 2>&1` -- should complete with zero errors. Verify all 9 sections present in built HTML by grepping for key strings: "JobFlow", "Lines of Code" (Stats), "What It Does" (Features), "Built With" (TechStack), "How It Works" (Architecture), "Under the Hood" (CodeSnippets), "Built in Public" (Timeline), "Quick Start" (QuickStart), and the footer GitHub link. Verify import count in index.astro (9 component imports: BaseLayout + 8 sections).
  </verify>
  <done>index.astro imports and renders all 9 sections (5 from Phase 21 + 4 from Phase 22) in correct order. Full site builds without errors. All sections visible in built HTML output.</done>
</task>

</tasks>

<verification>
1. `cd /Users/patrykattc/work/jobs/site && npm run build` completes with zero errors
2. `site/src/components/sections/Timeline.astro` contains `id="timeline"`, 3 milestones with correct stats
3. `site/src/components/sections/QuickStart.astro` contains `id="quickstart"`, 5 steps with `<Code />` blocks
4. `site/src/pages/index.astro` imports all 9 components and renders them in correct order
5. Built HTML contains all section headings and no two adjacent sections share the same background class
6. No horizontal page-level overflow -- all code blocks wrapped in `overflow-x-auto`
</verification>

<success_criteria>
- Timeline section tells the v1.0 -> v1.1 -> v1.2 build story with verified stats from PROJECT.md
- Quick Start section shows 5 actionable steps with syntax-highlighted bash commands
- Full page composes all 9 sections with alternating backgrounds and no visual merging
- Site builds and renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-engineering-depth-sections/22-02-SUMMARY.md`
</output>
